// React Native Firebase v24 — Modular API
// Native SDK configured via google-services.json (Android) and
// GoogleService-Info.plist (iOS). No manual initializeApp() needed.
// Import individual functions from their packages, not the default export.

export { getAuth } from '@react-native-firebase/auth';